```
git config --global credential.helper store
```

```
sudo git credential-store --file /root/.git-credential store
```

```
sudo git config --global credential.helper "store --file /root/.git-credentials"
```

